import 'package:flutter/material.dart';

class GoogleKeepPage extends StatelessWidget {
  const GoogleKeepPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Google Keep Page'),
      ),
    );
  }
}
