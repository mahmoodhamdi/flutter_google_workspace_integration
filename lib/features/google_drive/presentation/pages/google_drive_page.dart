import 'package:flutter/material.dart';

class GoogleDrivePage extends StatelessWidget {
  const GoogleDrivePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Google Drive Page'),
      ),
    );
  }
}
