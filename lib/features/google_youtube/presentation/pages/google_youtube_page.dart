import 'package:flutter/material.dart';

class GoogleYoutubePage extends StatelessWidget {
  const GoogleYoutubePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Google Youtube Page'),
      ),
    );
  }
}
