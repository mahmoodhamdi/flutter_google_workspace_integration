import 'package:flutter/material.dart';

class GoogleAnalyticsPage extends StatelessWidget {
  const GoogleAnalyticsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Google Analytics Page'),
      ),
    );
  }
}
