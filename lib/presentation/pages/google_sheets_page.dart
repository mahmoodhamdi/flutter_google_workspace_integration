import 'package:flutter/material.dart';

class GoogleSheetsPage extends StatelessWidget {
  const GoogleSheetsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Google Sheets Page'),
      ),
    );
  }
}
