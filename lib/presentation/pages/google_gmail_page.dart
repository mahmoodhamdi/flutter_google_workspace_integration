import 'package:flutter/material.dart';

class GoogleGmailPage extends StatelessWidget {
  const GoogleGmailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Google Gmail Page'),
      ),
    );
  }
}
