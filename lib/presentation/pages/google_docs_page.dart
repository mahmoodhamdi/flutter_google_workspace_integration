import 'package:flutter/material.dart';

class GoogleDocsPage extends StatelessWidget {
  const GoogleDocsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Google Docs Page'),
      ),
    );
  }
}
